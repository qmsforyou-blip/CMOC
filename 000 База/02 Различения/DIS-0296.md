---
id: "DIS-0296"
status: "NEW"
source: "unknown"
basis: "Следует различать **состояние объекта** и **историю объекта**. Состояние хранится в самом объекте, история изменений — в журнале событий."
patch: "PATCH-007"
tags: [различение, управление]
---

# DIS-0296: Следует различать **состояние объекта** и **историю объекта**. Состояние хранитс...

**Регистрация:**  
Следует различать **состояние объекта** и **историю объекта**. Состояние хранится в самом объекте, история изменений — в журнале событий.

**Источник:** не указан  
**Основание:** Следует различать **состояние объекта** и **историю объекта**. Состояние хранится в самом объекте, история изменений — в журнале событий.  
**Статус:** NEW  

## Связи
- Добавлено в смене: [[PATCH-007]]
