id: INV-0001

title:

statement:

formula:

mechanism:

why:                # Почему это работает?

consequences:       # Что неизбежно следует?

implementations:    # Возможные реализации

polygon:            # Как проверить воспроизводимость?

counterexamples:    # Кажущиеся исключения

limits:             # Границы применимости

related:
  inv:
  law:
  mech:
  entity:

origin:
  first_observation:
  generalized:
  confirmed:
  formalized:

sources:

maturity:

status:

history: